module.exports = {
    //http日志
    logger: {
        status: true,
    },

    //favicon设置
    favicon: {
        status: true,
    },

    //static配置
    static: {
        status: false,
    },

    //body配置
    body: {
        status: true,
    },

    //body配置
    view: {
        status: true,
    },

    //body配置
    router: {
        status: true,
    },

    //cors配置
    cors: {
        status: true,
    },

    //session配置
    session: {
        status: true,
    },

    //skip middleware
    skip: {
        status: true,
    },

    //http middleware
    http: {
        status: true,
    }
}
